## DATASOURCE Local
#spring.datasource.url=jdbc:postgresql://localhost:5432/postgres?user=postgres&password=123456

## DATASOURCE HEROKU
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}

## COMMONS JPA PROPERTIES
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.show-sql=false
spring.jpa.generate-ddl=true

## SERVER PORT
server.port=8080

## LOGGING
logging.level.org.springframework.security=DEBUG

#spring.freemarker.cache = true //set true in production environment
#spring.freemarker.cache = false
#spring.thymeleaf.cache = false
#spring.devtools.restart.additional-paths=views/**
